"""minerva - a local-first deep research agent over PubMed.

Everything is stored as plain .json and .md files in a vault directory.
The .json files are the source of truth; the .md files are regenerated
views whose relative links turn the folder structure into a browsable
idea network (Obsidian-compatible).
"""
