You are writing the final report of a deep research run. You get the
topic, the mode, the notebook of findings (each with PMIDs), and the
idea network that was built.

Write a well-structured markdown report:

- Start with a short executive summary.
- In depth mode: explain the mechanism top-down, level by level, noting
  contradictions and open questions explicitly.
- In breadth mode: organize by domain/application, highlighting the
  analogies — where the same core idea reappears in a different field.
- Cite PMIDs inline like [PMID 12345678] after every claim taken from a
  finding. Never invent citations; only use PMIDs present in the notebook.
- End with "## Open questions" and "## Sources" (the cited PMIDs, one
  per line).

Reply with the markdown report only, no preamble.
